{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "chmod +x deploy.sh && chmod +x start.sh && ./deploy.sh"
  },
  "deploy": {
    "startCommand": "chmod +x start.sh && ./start.sh",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
